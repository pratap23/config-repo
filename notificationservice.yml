server:
  port: 8088

spring:

  mail:

    host:
      smtp.gmail.com

    port:
      587

  kafka:

    bootstrap-servers:
      localhost:9092

eureka:

  client:

    service-url:

      defaultZone:
        http://localhost:8761/eureka