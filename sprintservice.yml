server:
  port: 8085

spring:

  datasource:

    url:
      jdbc:postgresql://localhost:5432/sprint_db

    username:
      postgres

    password:
      postgres

  kafka:

    bootstrap-servers:
      localhost:9092

  jpa:

    hibernate:

      ddl-auto:
        update

eureka:

  client:

    service-url:

      defaultZone:
        http://localhost:8761/eureka